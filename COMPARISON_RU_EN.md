# СРАВНЕНИЕ АРХИТЕКТУРЫ: Русский vs Английский сайты

## 📊 КРАТКОЕ СРАВНЕНИЕ

| Параметр | Русский (max2025.ru/course/) | Английский (Railway) |
|----------|------------------------------|---------------------|
| **Технологии** | PHP 8.2 + MySQL | Next.js 14 + PostgreSQL |
| **ORM** | PDO (нативный) | Prisma |
| **Уроков в seed** | ? (проверить БД) | 17 (в seed.ts) |
| **Уроков в конфиге** | 21 (файлы create_lesson21.php) | 22 (slides-config.json) |
| **Уроков на интерфейсе** | 21 | 21 |
| **Хранение контента** | MySQL LONGTEXT | PostgreSQL TEXT |
| **Хранение слайдов** | JSON поле в БД | JSON поле в БД + fallback |
| **Аудио** | /audio_lessons/*.mp3 | /public/audio/lesson-X/*.mp3 |
| **TTS** | Google TTS + ElevenLabs | ElevenLabs (Edge TTS backup) |

---

## 🔄 ДЕТАЛЬНОЕ СРАВНЕНИЕ

### 1. БАЗА ДАННЫХ

#### Русский сайт (MySQL)
```sql
CREATE TABLE lessons (
  id INT PRIMARY KEY,
  title VARCHAR(255),
  description TEXT,
  text_content LONGTEXT,      -- HTML контент
  audio_text LONGTEXT,         -- Текст для TTS
  audio_file VARCHAR(255),     -- Путь к MP3
  tts_voice VARCHAR(100),      -- ID голоса ElevenLabs
  order_num INT,
  duration_minutes INT,
  is_published TINYINT(1)
);
```

**Особенности:**
- ✅ `text_content` - готовый HTML для отображения
- ✅ `audio_text` - отдельный текст с маркерами [SLIDE:N]
- ✅ `audio_file` - ссылка на один большой MP3 файл
- ✅ Слайды НЕ хранятся отдельно в БД
- ✅ Слайды генерируются динамически из `audio_text`

#### Английский сайт (PostgreSQL)
```typescript
model Lesson {
  id            String
  courseId      String
  order         Int
  title         String
  description   String @db.Text
  content       String @db.Text    // Markdown
  duration      Int
  published     Boolean
  emoji         String
  color         String
  available     Boolean
  slides        Json?              // [{id, title, content}]
}
```

**Особенности:**
- ✅ `content` - Markdown, конвертируется в HTML на фронте
- ✅ `slides` - JSON массив слайдов (может быть пустым)
- ✅ Нет поля `audio_file` - аудио организовано по слайдам
- ✅ Если `slides` пустой → генерация из slides-config.json

---

### 2. АУДИО СИСТЕМА

#### Русский сайт
```
/course/audio_lessons/
  ├── lesson_1.mp3    (весь урок целиком)
  ├── lesson_2.mp3
  └── lesson_21.mp3
```

**Логика:**
- Один большой MP3 файл на урок
- В `audio_text` маркеры `[SLIDE:1]`, `[SLIDE:2]` и т.д.
- JavaScript парсит текст и синхронизирует с аудио
- При смене слайда - перематывает аудио на нужную позицию

**Пример:**
```php
$audioText = '[SLIDE:1]
Текст первого слайда...

[SLIDE:2]
Текст второго слайда...';
```

#### Английский сайт
```
/public/audio/
  ├── lesson-1/
  │   ├── slide-1.mp3
  │   └── (урок 1 имеет 1 слайд)
  ├── lesson-2/
  │   ├── slide-1.mp3
  │   ├── slide-2.mp3
  │   └── ... (11 слайдов)
  └── lesson-21/
      ├── slide-1.mp3
      ├── slide-2.mp3
      └── ... (19 слайдов)
```

**Логика:**
- Каждый слайд = отдельный MP3 файл
- При смене слайда - загружается новый файл
- Нет синхронизации - аудио проигрывается целиком

**API:**
```typescript
GET /api/audio/[lesson]/[slide]
→ возвращает /public/audio/lesson-{lesson}/slide-{slide}.mp3
```

---

### 3. СЛАЙДЫ

#### Русский сайт

**Хранение:**
- НЕТ отдельного поля для слайдов в БД
- Слайды извлекаются из `audio_text` по маркерам `[SLIDE:N]`

**Генерация:**
```php
// В lesson_v3.php
$slides = [];
$parts = preg_split('/\[SLIDE:\d+\]/', $audioText);
foreach ($parts as $index => $content) {
    $slides[] = [
        'id' => $index + 1,
        'content' => $content
    ];
}
```

**Результат:**
```javascript
slides = [
  { id: 1, content: "Текст первого слайда..." },
  { id: 2, content: "Текст второго слайда..." },
  ...
]
```

#### Английский сайт

**Хранение:**
- В БД поле `slides` (JSON nullable)
- Может быть заполнено или пустое

**Логика:**
```typescript
// Если в БД есть слайды
if (lesson.slides && lesson.slides.length > 0) {
  return lesson.slides;
}

// Если нет - генерируем из конфига
const slideCount = SLIDES_CONFIG[lessonOrder]; // например, 14
const slides = Array.from({ length: slideCount }, (_, i) => ({
  id: i + 1,
  title: `Part ${i + 1}`,
  content: lesson.content, // весь Markdown контент
  emoji: lesson.emoji,
  duration: 30000
}));
```

**Результат:**
```typescript
slides = [
  { id: 1, title: "Part 1", content: "# Lesson...", emoji: "📖" },
  { id: 2, title: "Part 2", content: "# Lesson...", emoji: "📖" },
  ...
]
```

---

### 4. СТАТИЧЕСКИЙ vs ДИНАМИЧЕСКИЙ КОНТЕНТ

#### Русский сайт

| Компонент | Источник | Тип |
|-----------|----------|-----|
| Список уроков | MySQL `lessons` | 🔄 Динамический |
| Название урока | MySQL `title` | 🔄 Динамический |
| HTML контент | MySQL `text_content` | 🔄 Динамический |
| Слайды | Парсинг `audio_text` | 🔄 Динамический |
| Аудио файл | Файл `/audio_lessons/lesson_X.mp3` | 📦 Статический файл |
| Вопросы квиза | MySQL `lesson_tasks` | 🔄 Динамический |

**Вывод:** Почти всё динамическое, аудио - статические файлы

#### Английский сайт

| Компонент | Источник | Тип |
|-----------|----------|-----|
| Список уроков | PostgreSQL `lessons` | 🔄 Динамический |
| Название урока | PostgreSQL `title` | 🔄 Динамический |
| Markdown контент | PostgreSQL `content` | 🔄 Динамический |
| Слайды | PostgreSQL `slides` → fallback: slides-config.json | 🔀 Гибридный |
| Количество слайдов | `/public/data/slides-config.json` | 📦 Статический конфиг |
| Аудио файлы | `/public/audio/lesson-X/slide-Y.mp3` | 📦 Статические файлы |
| Вопросы квиза | `/public/data/questions/lesson-X.json` | 📦 Статический JSON |

**Вывод:** Гибридная система - БД + статические конфиги и файлы

---

### 5. СОЗДАНИЕ УРОКОВ

#### Русский сайт

**Через скрипты:**
```php
// create_lesson21.php
$lessonId = 21;
$textContent = '<div class="lesson-intro">...</div>';
$audioText = '[SLIDE:1]\nТекст...\n[SLIDE:2]\n...';

// Вставка в БД
$pdo->prepare("INSERT INTO lessons ...")->execute([
  21, $title, $description, $textContent, 
  $audioText, $voiceId, 21, 25
]);

// Генерация аудио через ElevenLabs API
$audioData = generateAudio($audioText);
file_put_contents("audio_lessons/lesson_21.mp3", $audioData);
```

**Админка:**
- TinyMCE редактор для `text_content`
- Текстовое поле для `audio_text`
- Кнопка "Сгенерировать аудио"

#### Английский сайт

**Через админку:**
```typescript
// POST /api/admin/lessons
{
  order: 21,
  title: "Lesson 21",
  description: "...",
  content: "# Markdown content...",
  slides: [
    { id: 1, title: "Part 1", content: "...", audioText: "..." },
    { id: 2, title: "Part 2", content: "...", audioText: "..." }
  ]
}
```

**Через скрипты:**
```bash
# Генерация аудио для урока 21
node scripts/generate-lesson21-audio.js

# Создаёт файлы:
# /public/audio/lesson-21/slide-1.mp3
# /public/audio/lesson-21/slide-2.mp3
# ...
```

---

### 6. НАВИГАЦИЯ ПО УРОКАМ

#### Русский сайт
```
/course/lesson.php?id=21
         ↓
    lesson_v3.php
         ↓
   Загружает lesson из MySQL
         ↓
   Парсит audio_text → слайды
         ↓
   Загружает lesson_21.mp3
         ↓
   Синхронизирует аудио с слайдами
```

#### Английский сайт
```
/lessons/21
         ↓
  GET /api/lessons/21
         ↓
  PostgreSQL WHERE order=21
         ↓
  lesson.slides пустой?
    ДА → SLIDES_CONFIG[21] = 19
    НЕТ → использовать lesson.slides
         ↓
  Возвращает JSON с 19 слайдами
         ↓
  Фронт загружает аудио:
    /api/audio/21/1
    /api/audio/21/2
    ...
```

---

## 🎯 КЛЮЧЕВЫЕ РАЗЛИЧИЯ

### Философия архитектуры

**Русский сайт:**
- 🏛️ Монолитный PHP
- 📦 Всё в одном месте (БД + файлы)
- 🎵 Одно аудио на урок
- 📝 Слайды генерируются на лету

**Английский сайт:**
- 🧩 Модульный Next.js
- 🔀 Гибридная система (БД + конфиги + файлы)
- 🎵 Отдельное аудио для каждого слайда
- 📝 Слайды могут быть в БД или генерироваться

### Масштабируемость

**Русский сайт:**
- ➕ Проще добавить урок (один PHP скрипт)
- ➕ Всё в одной БД
- ➖ Большие MP3 файлы
- ➖ Сложная синхронизация аудио

**Английский сайт:**
- ➕ Гранулярное управление (слайд = файл)
- ➕ TypeScript = меньше ошибок
- ➖ Сложнее структура (больше файлов)
- ➖ Нужно заполнять slides-config.json

---

## 📌 ИТОГОВАЯ ТАБЛИЦА

| Критерий | Русский | Английский | Победитель |
|----------|---------|------------|-----------|
| **Простота создания урока** | ✅ Один скрипт | ❌ Много файлов | 🇷🇺 Русский |
| **Type Safety** | ❌ PHP | ✅ TypeScript | 🇬🇧 Английский |
| **Производительность БД** | ⚠️ MySQL | ✅ PostgreSQL | 🇬🇧 Английский |
| **Гибкость слайдов** | ❌ Только парсинг | ✅ БД или конфиг | 🇬🇧 Английский |
| **Размер аудио файлов** | ❌ Большие | ✅ Маленькие | 🇬🇧 Английский |
| **Простота поддержки** | ✅ Всё в БД | ❌ Много источников | 🇷🇺 Русский |
| **SEO** | ⚠️ PHP | ✅ Next.js SSR | 🇬🇧 Английский |

---

## 🚀 РЕКОМЕНДАЦИИ

### Для русского сайта:
1. ✅ Сохранить монолитную архитектуру
2. ⚡ Оптимизировать: разбить аудио на части
3. 📊 Добавить JSON поле для слайдов в БД

### Для английского сайта:
1. ✅ Сохранить модульную архитектуру
2. 📝 Заполнить `slides` в БД для всех уроков
3. 🧹 Удалить дублирование конфига (один источник истины)

---

**Создано:** 2026-02-08
**Автор:** Claude (AI Assistant)
