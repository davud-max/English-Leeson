# РЕШЕНИЕ: Заголовок не работает для уроков 21-27

## 🔍 Проблема

Ты изменил `[order]/page.tsx`, добавив заголовок под аудиоплеером:

```tsx
{/* Static Lesson Title */}
<div className="max-w-4xl mx-auto px-6 py-4 bg-amber-50 border-b border-amber-200">
  <h1 className="text-2xl font-bold text-center text-amber-800">
    {lesson?.title || `Lecture ${lessonOrder}`}
  </h1>
</div>
```

**Работает:** для уроков 1-15  
**НЕ работает:** для уроков 21-27

## 🎯 Причина

Это **client-side компонент**, поэтому проблема в одном из трёх:

1. ❌ Изменения НЕ закоммичены в Git
2. ❌ Изменения НЕ запушены на GitHub
3. ❌ Railway НЕ задеплоил новую версию
4. ❌ Браузер закэшировал старую версию

## ✅ РЕШЕНИЕ

### Шаг 1: Проверь Git статус

```bash
cd /Users/davudzulumkhanov/thinking-course-en
bash diagnose.sh
```

**Или вручную:**

```bash
git status
```

**Если есть незакоммиченные изменения:**

```bash
git add .
git commit -m "fix: add lesson title below audio player"
git push origin main
```

### Шаг 2: Проверь Railway

1. Открой https://railway.app
2. Найди свой проект "English-Leeson"
3. Проверь статус деплоя
4. Если деплой завершён, но изменения не видны → **перезапусти деплой**

**Принудительный редеплой:**
```bash
# Сделай пустой коммит
git commit --allow-empty -m "trigger redeploy"
git push origin main
```

### Шаг 3: Очисти кэш браузера

**Для уроков 21-27:**

1. Открой урок 21: https://your-app.up.railway.app/lessons/21
2. Нажми `Cmd + Shift + R` (Mac) или `Ctrl + Shift + R` (Windows)
3. Повтори для уроков 22-27

**Или через DevTools:**

1. Открой урок 21
2. Нажми `F12` (DevTools)
3. Правый клик на кнопке перезагрузки
4. Выбери "Empty Cache and Hard Reload"

### Шаг 4: Проверь Next.js Router Cache

Если проблема сохраняется, это может быть Next.js Router Cache.

**Добавь параметр к URL:**
```
https://your-app.up.railway.app/lessons/21?v=2
```

Это заставит Next.js пропустить кэш.

## 🔬 ДИАГНОСТИКА

### Если изменения ВИДНЫ в DevTools, но не на странице:

Это **клиентский кэш**. Решение:
- Hard reload (`Cmd + Shift + R`)
- Инкогнито режим

### Если изменения НЕ ВИДНЫ в DevTools Source:

Это проблема **деплоя**. Решение:
- Проверь Railway Deploy Logs
- Проверь Build Logs
- Принудительно редеплой

### Если работает ТОЛЬКО для 1-15, но НЕ для 21-27:

Возможные причины:

1. **Уроки 21-27 не в БД** (но ты сказал, что они работают)
2. **Разный source code для разных уроков** (проверь условную логику)
3. **Кэш на уровне CDN** (если используешь CDN перед Railway)

## 🛠️ БЫСТРОЕ РЕШЕНИЕ

```bash
cd /Users/davudzulumkhanov/thinking-course-en

# 1. Убедись, что изменения закоммичены
git add src/app/\(course\)/lessons/\[order\]/page.tsx
git commit -m "fix: add lesson title under audio player"
git push origin main

# 2. Дождись деплоя на Railway (~2-3 минуты)

# 3. Открой урок 21 с принудительной перезагрузкой
# Cmd + Shift + R
```

## 📊 ПРОВЕРКА

После применения решения:

```bash
# Открой каждый урок из группы 21-27
open "https://your-app.up.railway.app/lessons/21"
open "https://your-app.up.railway.app/lessons/22"
open "https://your-app.up.railway.app/lessons/23"
# и т.д.

# С hard reload в каждом
```

Заголовок должен появиться под аудиоплеером.

## 💡 СОВЕТ

Чтобы избежать этого в будущем:

1. **Всегда делай hard reload** после деплоя
2. **Используй инкогнито режим** для тестирования
3. **Добавь версию в URL** при тестировании новых изменений
4. **Проверяй Railway Deploy Logs** перед тестированием

---

**Запусти:** `bash diagnose.sh` чтобы узнать, на каком шаге проблема.
